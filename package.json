{
  "name": "@lunarity/nebula-fetch-cli",
  "module": "index.ts",
  "description": "CLI tool for downloading media from different platforms",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@ybd-project/ytdl-core": "latest",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "bin": {
    "nebula-fetch": "./index.ts"
  },
  "scripts": {
    "build": "bun build ./index.ts --compile --outfile nebula-fetch",
    "dev": "bun index.ts",
    "prepublish": "bun run build"
  }
}
